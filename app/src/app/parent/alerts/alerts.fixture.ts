/**
 * 보호자 알림함 문구 — 어긋남 대장 D51.
 *
 * 🔴 **알림함이 원본이다.** 여기 한 줄이 남는 것이 「알렸다」의 정의이고,
 *    웹 푸시는 그것을 폰에 띄우는 사본이다 (어긋남 대장 D56).
 *
 * 🔴 **켜졌는지 안 켜졌는지를 화면이 말한다.** 안 적으면 안 켜 둔 부모는
 *    「왜 폰에 안 왔지」로, 켜 둔 부모는 「이게 다인가」로 읽는다.
 *    메일·문자·알림톡은 여전히 없다 — 그것도 적는다.
 */

export const title = "알림";

export const empty = {
  emoji: "🔔",
  title: "새 알림이 없어요",
  body: "아이가 미션을 끝내고 하루가 지나면 여기로 알려드립니다.",
  hint: "72시간이 지나면 자동으로 완료됩니다",
};

/**
 * 🔴 두 문구를 **상태에 따라** 고른다 (D56). 하나로 두면 반드시 한쪽에 거짓이 된다.
 *    문자·메일·알림톡은 어느 쪽이든 없다 — 그 사실은 양쪽에 남긴다.
 */
export const pushNotice = {
  on: "폰 알림이 켜져 있어요. 문자·메일·알림톡으로는 가지 않습니다.",
  off: "지금은 이 앱 안에서만 보입니다. 「내 정보 → 폰 알림」에서 켜면 폰 잠금화면에도 뜹니다.",
};

export const readNotice = "열어 보신 알림은 읽음으로 표시됩니다.";
