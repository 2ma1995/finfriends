/**
 * 자녀 초대 화면 문구 — CON-003 · 온보딩 4단계.
 *
 * 🔴 **아이 데이터는 여기 없다.** DB 에서 읽는다 (`findChild`). 이 파일은 문구만 갖는다.
 *
 * 🔴 초대 「링크」를 만들지 않는다. 발급 함수(`issueDeviceToken`)는 **그 기기에서 호출될 때**
 *    쿠키를 심는 방식이고, 링크로 토큰을 나르는 경로는 구현하지 않았다.
 *    있는 것처럼 적으면 부모가 링크를 찾다가 막힌다 — 그래서 실제 동선을 적는다.
 */

/** 아이 기기에서 부모가 직접 한다 — 아이에게 무언가를 보내는 것이 아니다 */
export const howTo = [
  "아이가 쓸 기기에서 핀프렌즈를 엽니다.",
  "그 기기에서 부모 계정으로 로그인합니다.",
  "「기기 등록」을 누르면 그 기기가 아이 화면으로 바뀝니다.",
  "등록이 끝나면 그 기기의 부모 로그인은 자동으로 끝납니다.",
];

/** 스킬 304 §4 — 「아동 독립 로그인 시도 0건」은 인증 구조에서 결정된다 */
export const noCredentialNotice = {
  title: "아이는 아이디도 비밀번호도 만들지 않아요",
  body: "등록되는 것은 기기입니다. 아이가 로그인하는 일은 없고, 아이 화면은 부모 계정 안의 프로필로 열립니다.",
};

/**
 * 기기 등록 규칙. 어긋남 대장 D5·D5-b 의 결정을 화면 문구로 옮긴 것이다.
 * 값의 원천은 `src/lib/session/device-session.ts` (TTL_DAYS=180 · verifyChildAccess).
 */
export const rules: readonly { k: string; v: string }[] = [
  { k: "등록 유지", v: "180일 · 해제할 때까지" },
  { k: "부모 로그아웃", v: "아이 기기는 그대로" },
  { k: "열리는 곳", v: "아이 화면만" },
  { k: "부모 화면", v: "보호자 PIN" },
  { k: "동의", v: "들어올 때마다 확인" },
];

export const previewLink = { href: "/join", label: "이 기기를 아이 기기로 등록하기" };

/**
 * 🔴 **아이 폰에 부모 비밀번호를 치게 하지 않는다** (D63).
 *    「쓰는 법」은 그 기기에서 부모로 로그인하라고 하는데, 그건 아이 앞에서
 *    부모 비밀번호를 치는 흐름이다. 링크를 보내면 그럴 일이 없다.
 */
export const inviteLink = {
  make: "초대 링크 만들기",
  remake: "링크 다시 만들기",
  hint: "아이 폰에 보내면 그 폰이 아이 화면이 됩니다. 24시간 · 한 번만 쓸 수 있어요.",
  copyLabel: "링크 복사",
  copied: "복사했어요",
  failed: "복사가 안 됐어요. 주소를 직접 골라 복사해 주세요.",
};

/**
 * 기기를 **다시** 등록할 때 — 어긋남 대장 D65.
 *
 * 🔴 아이 화면이 이상하게 나올 때 부모가 스스로 되돌릴 수 있어야 한다
 *    (2026-09-02 사용자 요청). 해제와 재등록이 각각 있었지만 **사이가 끊겨 있었다** —
 *    해제한 뒤 재등록으로 가는 길이 화면에 없었다.
 *
 * 🔴 **기록은 그대로다.** 용돈·별·나무는 아이에게 붙어 있고 기기에 붙어 있지 않다.
 *    이 말을 안 적으면 부모가 해제를 무서워해서 못 고친다.
 */
export const reRegisterNotice = {
  title: "기기 다시 등록",
  sub: "아이 화면이 안 열릴 때",
  body: "새 링크를 만들어 아이 기기에서 한 번 열면 다시 등록됩니다. 용돈·별·나무 기록은 그대로 이어져요.",
};

/**
 * 링크를 넘긴 뒤 기다리는 동안 — 어긋남 대장 D76.
 *
 * 🔴 **기다리라고 말해 준다.** 안 적으면 부모는 링크를 다시 만들거나 뒤로 나간다 —
 *    화면이 아무 말도 안 하면 「안 되는구나」로 읽힌다.
 */
export const waitNotice = {
  waiting: "아이 기기에서 링크를 열면 이 화면이 다음 단계로 넘어갑니다.",
  done: "등록됐어요. 다음 단계로 넘어갑니다…",
};
