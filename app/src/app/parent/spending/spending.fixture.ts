/**
 * 소비 내역 화면 문구 — PLN-005.
 *
 * 🔴 **데이터는 여기 없다.** `modules/plan.getSpendSummary` 가 DB 에서 읽는다.
 *    예전에는 이 파일이 4만 7천원·업종 4줄을 값째로 갖고 있어서
 *    **새로 가입한 계정도 남의 소비를 봤다.**
 */

/**
 * 🔴 **잘못을 표시하는 화면이 아니다.** ⭐ 판정은 금액 단독이고(ADR-008),
 *    업종이 어긋난 것은 회고 문장을 갈라놓을 뿐 별을 막지 않는다.
 *    문구가 이 구분을 흐리면 부모가 아이를 다그치는 화면이 된다.
 */
export const notice = "계획에 없던 업종을 표시하는 것이지, 잘못을 표시하는 것이 아닙니다.";

/**
 * 🔴 **달이 바뀌면 어제 쓴 것이 이번 달 목록에서 빠진다.**
 *    그때 「소비 내역이 없어요 · 카드를 연결하면 모입니다」라고 말하면
 *    보호자는 **기록이 안 된 줄 안다.** 매달 1일마다 고장 난 것처럼 보였다.
 *    기록이 있는데 이번 달이 아닐 뿐이라는 것을 그대로 말한다.
 */
export const monthEmpty = {
  emoji: "🗓",
  title: "이번 달은 아직 쓴 게 없어요",
  body: "달이 바뀌어서 이번 달 기록은 아직 비어 있습니다. 지난달 기록은 아래에 있어요.",
  hint: "기록이 사라진 게 아닙니다",
};
export const prevRecordsTitle = "지난달 내역";

/** 결제 웹훅(PTN-002)이 아직 없어 카드 내역이 들어오지 않는다 */
export const emptyState = {
  emoji: "💳",
  title: "이번 달 소비 내역이 없어요",
  body: "카드를 연결하면 아이가 쓴 내역이 여기에 모입니다.",
  hint: "계획 카드를 먼저 적어두면 쓴 뒤에 겹쳐 볼 수 있어요",
};

/** 지난달 기록이 없을 때 — 증감을 0으로 그리지 않는다 */
export const noPrevNotice = "이번 달 합계 · 지난달 기록이 없어 비교는 다음 달부터";

/**
 * 계획 없이 나간 소비 = C5 사각지대. 🔴 **크기를 숨기지 않는다.**
 * 계획 카드 작성률이 F8 의 생존 조건이라(O7 ≥50%) 이 수가 곧 경고다.
 */
export const noPlanNotice = (n: number) =>
  `${n}건은 계획 카드 없이 쓴 소비예요. 가기 전에 적어두면 쓰고 나서 겹쳐 볼 수 있어요.`;

/**
 * 건별 내역 — 어긋남 대장 D26.
 *
 * 🔴 **잘못을 표시하는 목록이 아니다.** 세 문구 전부 사실 진술이고,
 *    어느 것도 별을 막지 않는다 (ADR-008). 색으로 가르지 않는 것도 같은 이유다 —
 *    「계획 없이」에 경고색을 쓰면 부모가 아이를 다그치는 화면이 된다 (P-03).
 */
export const recordsTitle = "건별 내역";

/** 🔴 가게 이름이 없는 이유를 부모에게 말한다. 안 적으면 「왜 안 나오지」가 된다 */
export const recordsNotice =
  "가게 이름은 남기지 않습니다. 아이가 어디를 다니는지가 그대로 기록이 되기 때문이에요.";

/** 결제 웹훅(PTN-002)이 아직 없다 — 지금 보이는 것이 어디서 왔는지 숨기지 않는다 */
export const seedNotice = "카드 연결 전이라 예시 데이터입니다.";

