/**
 * 마이페이지 문구.
 *
 * 🔴 **데이터는 여기 없다.** `modules/account.getMyPage` 가 DB 에서 읽는다.
 */

export const deviceNotice = {
  empty: "등록된 아이 기기가 없어요. 아이가 쓸 기기에서 로그인해 「기기 등록」을 누르면 여기에 나타납니다.",
  /** 🔴 D5-b — 해제는 즉시 듣는다. 쿠키가 남아 있어도 다음 진입에서 막힌다 */
  hint: "해제하면 그 기기의 아이 화면이 바로 잠깁니다. 다시 등록하면 기록은 그대로 이어져요.",
};

/**
 * 🔴 **시연용 가짜다.** 실제 발급은 제휴사가 하고(PTN-001 · §6.1 진입점 9번),
 *    착수 조건인 D1(수수료율·SLA·업종 코드)과 D-03(본인인증 위임)이 둘 다 미확정이다.
 *    화면에도 가짜임을 적는다 — 시연에서 「이미 되는 기능」으로 오해되면
 *    제휴 협상 전에 약속이 생긴다.
 */
export const cardNotice = {
  mock: "🔴 시연용 화면입니다. 실제 카드가 발급되지 않습니다.",
  body: "실제 발급은 제휴 선불업자가 맡습니다. 수수료·이용한도·본인확인 방식이 정해지면 이 자리가 실제 신청 흐름으로 바뀝니다.",
};

/**
 * 🔴 D5 의 「아직 안 한 것」 — PIN 설정·검증 흐름과 모드 전환 UI 가 없다.
 *    컬럼(`child_mode_pin_hash`)은 있고 시드가 `1234` 를 넣어 두었다.
 */
export const pinNotice = {
  body: "아이 기기에서 부모 화면으로 돌아올 때 쓰는 번호입니다. 아이가 부모 비밀번호를 알게 되지 않도록 PIN 을 따로 둡니다.",
  todo: "설정·변경 화면을 준비하고 있습니다.",
};

/** 동의 화면과 같은 규약 — 무엇을 받지 않는지 한 번 더 적는다 */
export const notCollected = ["카드번호", "계좌", "실명", "위치 정보", "얼굴 사진"];
