/**
 * 마이페이지 문구.
 *
 * 🔴 **데이터는 여기 없다.** `modules/account.getMyPage` 가 DB 에서 읽는다.
 */

export const deviceNotice = {
  /**
   * 🔴 **낡은 안내였다** (어긋남 대장 D65). 「아이가 쓸 기기에서 **로그인해**
   *    「기기 등록」을 누르면」이라고 적혀 있었다 — 그건 초대 링크가 생기기 전 방식이고,
   *    **아이 폰에 부모 비밀번호를 치라는 말**이 된다. 링크를 만든 이유가 그걸 없애는 것이었다.
   */
  empty: "등록된 아이 기기가 없어요. 초대 링크를 만들어 아이 기기에서 한 번 열면 등록됩니다.",
  /** 🔴 D5-b — 해제는 즉시 듣는다. 쿠키가 남아 있어도 다음 진입에서 막힌다 */
  hint: "해제하면 그 기기의 아이 화면이 바로 잠깁니다. 다시 등록하면 기록은 그대로 이어져요.",
  /** 🔴 아이 화면이 이상할 때 부모가 스스로 되돌리는 길 (2026-09-02 사용자 요청) */
  reRegister: "초대 링크 만들기",
  revoked: "기기를 해제했어요. 아이 화면은 지금 잠겨 있습니다. 아래 「초대 링크 만들기」로 다시 등록하면 기록은 그대로 이어져요.",
};

/**
 * 🔴 **시연용 가짜다.** 실제 발급은 제휴사가 하고(PTN-001 · §6.1 진입점 9번),
 *    착수 조건인 D1(수수료율·SLA·업종 코드)과 D-03(본인인증 위임)이 둘 다 미확정이다.
 *    화면에도 가짜임을 적는다 — 시연에서 「이미 되는 기능」으로 오해되면
 *    제휴 협상 전에 약속이 생긴다.
 */
export const cardNotice = {
  mock: "🔴 시연용 화면입니다. 실제 카드가 발급되지 않습니다.",
  body: "실제 발급은 제휴 선불업자가 맡습니다. 수수료·이용한도·본인확인 방식이 정해지면 이 자리가 실제 신청 흐름으로 바뀝니다.",
};

/**
 * 아동 모드 PIN — `D5` · 어긋남 대장 D41.
 *
 * 🔴 컬럼은 처음부터 있었는데 **읽기만 하고 아무도 넣지 않았다.** 그래서 아동 모드를
 *    푸는 유일한 길이 로그인이었고, 로그인하면 **아이 기기 등록이 풀린다**(`D27`).
 */
export const pinNotice = {
  body: "아이 기기에서 부모 화면을 잠깐 볼 때 쓰는 번호입니다. 아이 기기에서 비밀번호를 치면 아이가 그 비밀번호를 알게 되므로 PIN 을 따로 둡니다.",
  /** 🔴 PIN 이 있으면 기기 등록이 안 풀린다는 것을 말한다 — 그게 이걸 정하는 이유다 */
  why: "PIN 을 정해 두면 아이 기기 등록을 풀지 않고도 부모 화면을 볼 수 있어요.",
  /** 🔴 잠깐만 열린다는 것과, 여러 번 틀리면 잠긴다는 것을 미리 말한다 */
  rule: "10분 뒤 자동으로 잠기고, 다섯 번 틀리면 로그인으로만 풀 수 있습니다.",
};

export const pinLabel = "네 자리 숫자";
export const pinSetLabel = "PIN 정하기";
export const pinChangeLabel = "PIN 바꾸기";
export const pinClearLabel = "PIN 지우기";
/** 🔴 지우면 무슨 일이 생기는지 말한다 — 「없앤다」만 적으면 대가가 안 보인다 */
export const pinClearNotice = "지우면 아이 기기에서 부모 화면을 보려면 로그인해야 하고, 그때 기기 등록이 풀립니다.";

export const pinDone: Record<string, string> = {
  set: "PIN 을 정했어요.",
  cleared: "PIN 을 지웠어요.",
};
export const pinErrors: Record<string, string> = {
  BAD_FORMAT: "네 자리 숫자로 넣어 주세요.",
  /** 🔴 아이가 첫 번째로 눌러 보는 것들이다. 막지 않으면 잠금이 아니라 장식이다 */
  TOO_SIMPLE: "0000 · 1234 처럼 이어지거나 같은 숫자는 아이가 먼저 눌러 봅니다. 다른 번호로 정해 주세요.",
};

/** 동의 화면과 같은 규약 — 무엇을 받지 않는지 한 번 더 적는다 */
export const notCollected = ["카드번호", "계좌", "실명", "위치 정보", "얼굴 사진"];

/**
 * 하교 시각 — 어긋남 대장 D41.
 *
 * 🔴 **계획 카드의 가장 큰 구멍이 「적으라고 말할 자리가 없다」였다.**
 *    아이가 그냥 나가면 대조할 것이 없고(`C5` 사각지대),
 *    계획 작성률(`O7` ≥50%)이 이 기능의 생존 조건이다.
 */
export const schoolNotice = {
  title: "하교 시각",
  body: "이 시각이 지났는데 오늘 쓸 계획을 아직 안 적었으면, 아이 화면에서 한 번 물어봅니다.",
  /** 🔴 하루에 한 번만 묻는다는 것을 말한다 — 계속 뜨는 줄 알면 꺼 두고 싶어진다 */
  once: "하루에 한 번만 묻고, 아이가 계획을 적었으면 묻지 않습니다.",
  /**
   * 🔴 **바꾸면 그날 다시 묻는다.** 안 적으면 3시로 잘못 넣었다 2시로 고친 날
   *    영영 안 물어 하루를 통째로 잃는다.
   */
  change: "시각을 바꾸면 그날은 다시 한 번 묻습니다.",
  empty: "아직 정하지 않으셨어요. 정하지 않으면 묻지 않습니다.",
};
export const schoolSaveLabel = "저장";
export const schoolDone = "하교 시각을 정했어요.";
export const schoolErrors: Record<string, string> = {
  BAD_TIME: "시각을 확인해 주세요.",
  NOT_MINE: "등록한 아이를 찾지 못했어요.",
  NO_CHILD: "먼저 아이를 등록해 주세요.",
};
