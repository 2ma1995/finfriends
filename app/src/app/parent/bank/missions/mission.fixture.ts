// PROTO-DATA: PRC-001 · PRC-003 — 🔴 데이터는 DB(`@/modules/mission`)가 준다. 여기 남은 것은 문구뿐이다.

/** 5건 이상이면 일괄 승인이 열린다 (PRC-003) */
export const BULK_THRESHOLD = 5;

/** 소급 지급 — 승인하면 한 날짜 기준으로 반영된다 (PRC-002) */
export const retroNotice = {
  title: "아직 반영되지 않았어요",
  body: "아이는 이미 했습니다. 승인하면 한 날짜 기준으로 소급해서 반영됩니다",
};

/** 🔴 아이가 스스로 올린 것은 갈라 보여준다 — 보호자가 자기가 시킨 줄 알면 안 된다 */
export const fromLessonBadge = "아이가 배우고 스스로 함";

export const approveLabel = "승인";
export const rejectLabel = "아니요";
export const bulkLabel = "모두 승인";
/** 🔴 사유 없이 거절하지 않는다 — 아이 화면에서 「미실천」과 구별되지 않는다 (AC-6.2) */
export const reasonPlaceholder = "거절 사유를 적어주세요";

export const empty = { title: "기다리는 미션이 없어요", body: "아이가 미션을 마치면 여기에 올라옵니다" };
export const needLogin = { title: "로그인이 필요해요", body: "보호자 계정으로 로그인해 주세요" };

/**
 * 미션 사진 — `FR-032` · 어긋남 대장 D32.
 *
 * 🔴 **판정하면 사진이 사라진다는 것을 누르기 전에 말한다.** 안 적으면 보호자는
 *    나중에 다시 볼 수 있는 줄 알고, 못 찾으면 고장으로 읽는다.
 *
 * 🔴 이 기능은 이전 사양에서 **아동 이미지 리스크로 제외**돼 있었다. 「판정 즉시 파기」가
 *    다시 넣은 조건이므로, 화면도 그 약속을 그대로 말해야 한다.
 */
export const photoNotice = "승인하거나 돌려보내면 이 사진은 바로 지워집니다. 다시 볼 수 없어요.";
export const photoAlt = "아이가 올린 미션 사진";

/**
 * 24시간 넘게 기다리는 미션 — `FR-032` 「24h 미승인 → 리마인드 1회」.
 *
 * 🔴 **알림 인프라가 없다.** 푸시도 메일도 없어서 화면 표시로 대신한다.
 *    없는 것을 있는 척하지 않는다 (어긋남 대장 D37).
 *
 * 🔴 다그치는 문구를 쓰지 않는다 — 부모가 바쁜 것은 잘못이 아니다.
 *    말해야 하는 것은 **아이가 기다리고 있다**는 사실이다.
 */
export const overdueNotice = (n: number) =>
  `${n}건은 하루 넘게 기다리고 있어요. 사흘이 지나면 자동으로 닫히고 별은 지급되지 않습니다.`;

/** 🔴 기다림에 끝이 있다는 것을 부모도 알아야 한다 */
export const expireNotice = "「했어요」를 누른 뒤 사흘 안에 확인해 주세요.";

/**
 * 🔴 **사유 없이 거절하지 않는다** (`AC-6.2`).
 *    사유가 없으면 아이 화면에서 **「미실천」과 구별되지 않고**, 아이에게는
 *    「했는데 왜」만 남는다.
 *
 * 🔴 입력칸에 `required` 를 걸 수 없다 — 「칭찬하기」와 같은 폼이라
 *    승인까지 막힌다. 그래서 **서버가 검사하고 여기로 돌려보낸다.**
 */
export const reasonRequired = "돌려보내려면 이유를 적어 주세요. 아이 화면에 그대로 보입니다.";

/** 🔴 아직 못 본 미션에 붙인다. 부모가 화면을 열면 다음부터 안 붙는다 (D52) */
export const newBadge = "새로 왔어요";

/**
 * 사진 없이 올라온 **벌기 · 부모 미션** — 🔴 사용자 지적으로 열린 길.
 *
 * 이 영역은 사진이 필수인데, 아이가 「부모님이 직접 보실래요」를 고르면
 * 사진 없이 올라온다. **부모가 그걸 모르면 근거 없이 승인하게 된다** —
 * 사진칸이 그냥 비어 보일 뿐이라 「아직 안 올라왔나」로 읽힌다.
 *
 * 🔴 아이를 의심하게 만드는 문구를 쓰지 않는다. **할 일을 적는다.**
 */
export const checkInPersonNotice = {
  title: "직접 보고 확인해 주세요",
  body: "아이가 사진 대신 직접 확인을 골랐어요. 보시고 승인해 주세요.",
};
