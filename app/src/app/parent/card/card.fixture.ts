/**
 * 카드 신청 화면 문구 — 🔴 시연용. 어긋남 대장 D20.
 *
 * 🔴 **데이터는 여기 없다.** 단계는 `modules/account` 가 DB 에서 읽는다.
 */

/**
 * 🔴 화면 맨 위에 둔다. 시연에서 「이미 되는 기능」으로 오해되면
 *    제휴 협상 전에 약속이 생긴다. 착수 조건 D1(수수료·SLA·업종 코드)과
 *    D-03(본인인증 위임)이 둘 다 미확정이다.
 */
export const mockBanner =
  "🔴 시연용 화면입니다. 실제 카드가 발급되지 않고, 어떤 정보도 제휴사로 전송되지 않습니다.";

/** US-8 AC2 · F16 — 카드 배송 대기 중에도 학습·퀴즈·별은 열린다 */
export const whileWaiting = {
  title: "카드를 기다리는 동안에도 시작할 수 있어요",
  body: "배우기와 퀴즈로 별을 받을 수 있습니다. 카드가 필요한 계획 카드·소비 내역만 잠깁니다.",
};
