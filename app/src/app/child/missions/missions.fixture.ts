// PROTO-DATA: PRC-001 — 🔴 데이터는 DB(`@/modules/mission`)가 준다. 여기 남은 것은 문구뿐이다.

/**
 * 🔴 **이 화면에는 두 가지가 섞여 있다.** 어제 「부모님이 만들어 주신 미션이에요」라고
 *    적었는데 **틀린 말이었다** — 실천하기에서 「해봤어요」를 누른 것도 같은 표에 쌓인다
 *    (`claimPractice` 가 `missions` 행을 만든다). 그래서 줄마다 어디서 왔는지 말한다.
 */
export const intro = "부모님이 준 미션과, 배우고 스스로 한 것이 함께 있어요.";

/** 🔴 어디서 온 줄인지 — 아이는 「내가 한 것」과 「시킨 것」을 구별할 수 있어야 한다 */
export const source = {
  parent: "부모님이 줌",
  lesson: "배워서 한 것",
};

/**
 * 🔴 **지난 미션은 딴 화면이다** (사용자 지적). 끝난 것이 여기 쌓이면
 *    **오늘 할 것이 매일 아래로 밀린다** — 아이가 매일 여는 화면이다.
 */
export const pastLink = "지난 미션 보기";
export const pastEmpty = { title: "아직 지난 미션이 없어요", body: "미션을 끝내면 여기에 쌓여요" };

export const sections = {
  todo: "오늘 할 수 있는 것",
  waiting: "부모님 확인을 기다려요",
  settled: "지난 미션",
};

export const doneLabel = "했어요";
/**
 * 🔴 사진은 **선택**이다 (FR-032). 찍을 수 없는 실천(참기·기록하기)도 있다.
 * 🔴 **부모가 보고 나면 바로 지워진다**는 것을 아이에게 말한다 — 아동 이미지다.
 */
/**
 * 🔴 **벌기 부모 미션만 사진이 필수다** (D49). 「안 해도 돼요」를 지웠다 —
 *    필수인데 안 해도 된다고 적으면 아이가 그냥 눌렀다가 막힌다.
 */
export const photoLabel = "사진 보여주기 (꼭 있어야 해요)";
/** 🔴 선택인 칸은 **안 해도 된다고 적는다.** 필수와 같은 말을 쓰면 아이가 눌렀다가 막힌 줄 안다 */
export const photoLabelOptional = "사진 보여주기 (안 해도 돼요)";
export const photoNotice = "부모님만 보시고 바로 지워져요.";

/**
 * 🔴 **완료 뒤에도 붙일 수 있어야 한다.** 예전엔 사진칸이 「했어요」 버튼과 같은 폼에만
 *    있어서, 한 번 누르고 나면 붙일 방법이 **아예 없었다.**
 *    아이는 **하고 나서** 찍는다 — 누르기 전에 찍어 두라는 건 어른의 순서다.
 */
export const photoLater = "사진 나중에 보여주기";

/**
 * 🔴 **사진을 왜 받는지, 왜 안 받는지 말한다.** 어떤 미션은 사진칸이 있고
 *    어떤 것은 없으면 아이는 「왜 다르지」가 된다. 이유가 각각 다르다.
 */
export const photoWhy = "부모님이 볼 수 없는 일이라서 사진이 있어야 해요.";
/**
 * 🔴 **선택인데 왜 칸이 있는지** 말한다. 벌기는 앱이 볼 수 없는 일이라 사진이 도움이
 *    되지만, **아이가 스스로 고른 실천**이라 강요하지 않는다 — 안 붙이면 부모가 물어본다.
 */
export const photoWhyOptional = "보여주고 싶으면 붙여요. 안 붙여도 부모님이 물어보고 확인해 주세요.";
/** 🔴 벌기는 여기 없다 — 벌기는 부모 미션이든 배워서 한 것이든 **사진칸이 있다** */
export const noPhotoWhy: Partial<Record<string, string>> = {
  SPEND: "카드로 쓴 내역이 부모님께 보여요. 사진은 안 찍어도 돼요.",
  SAVE: "통장에 모인 돈이 부모님께 보여요. 사진은 안 찍어도 돼요.",
  GROW: "저금한 것이 부모님께 보여요. 사진은 안 찍어도 돼요.",
};
export const photoAttached = "📷 사진을 보여줬어요";
export const photoReplace = "다른 사진으로 바꾸기";

/** 🔴 실패를 조용히 넘기지 않는다. 「올라갔겠지」로 넘어가면 안 된다 */
export const photoResult: Record<string, string> = {
  ok: "사진을 보여줬어요. 부모님이 보시면 바로 지워져요.",
  BAD_MIME: "이 사진은 못 올려요. 사진으로 다시 찍어 볼래요?",
  TOO_LARGE: "사진이 너무 커요. 조금 작게 찍어 볼래요?",
  EMPTY: "사진이 비어 있어요. 다시 골라 볼래요?",
  NOT_FOUND: "이 미션에는 지금 사진을 붙일 수 없어요.",
  /** 🔴 「안 돼요」로 끝내지 않는다 — 무엇을 하면 되는지 적는다 (ACE-1.1) */
  NEED: "이 미션은 사진이 있어야 해요. 한 장 찍어서 같이 보내 주세요.",
};
export const undoLabel = "아니에요";

/** 🔴 「승인 대기」를 「미실천」과 구별해 말한다 (AC-6.2) */
export const waitingNotice = "이미 한 거예요. 부모님이 보시면 별이 붙어요.";
/** 🔴 거절도 「안 했다」가 아니다. 사유를 그대로 보여준다 */
export const rejectedPrefix = "이번엔 별이 안 붙었어요";
/**
 * 72시간이 지나도록 부모가 못 본 것 — `FR-032` · `AC-032-3`.
 *
 * 🔴 **거절과 같은 자리에 두지 않는다.** 아이에게 둘은 완전히 다른 일이다.
 *
 *      거절   부모가 **보고** 아니라고 했다 — 사유가 있다
 *      만료   부모가 **못 봤다** — 아이는 잘못한 게 없다
 *
 *    거절색(`miss`)을 쓰면 아이는 **한 일을 부정당한 것**으로 읽는다.
 *    「거절」·「실패」라는 말도 쓰지 않는다.
 *
 * 🔴 **「다시 할 수 있어요」라고 말하지 않는다.** `markDone` 이 `PENDING` 만 받아서
 *    만료된 미션은 **아이가 다시 못 누른다.** 거짓말이 되고, 눌러 보고 안 되면
 *    아이는 앱을 못 믿게 된다. 대신 **실제로 할 수 있는 것**을 적는다 —
 *    부모에게 말하는 것. 다시 걸어 주는 것은 부모가 할 수 있다.
 */
export const expiredTitle = "부모님이 확인하지 못했어요";
export const expiredBody = "네가 한 건 맞아요. 부모님께 한 번 말해 볼까요?";

/**
 * 72시간이 지나 **자동으로 완료된 것** — `D50` · `D51`.
 *
 * 🔴 **「부모님이 확인했어요」라고 말하면 안 된다.** 부모는 못 봤다.
 *    별을 받은 건 같지만 **누가 인정했는가는 다르고**, 그게 이 제품의 근거다.
 *    아이가 「엄마가 봤구나」로 읽으면, 나중에 부모가 「그거 못 봤는데」라고 할 때
 *    **아이는 앱이 거짓말했다고 느낀다.**
 *
 * 🔴 **아이 탓으로 들리지 않게 한다.** 기다림이 끝난 것이지 아이가 뭘 잘못한 게 아니다.
 */
export const autoDoneNotice = "기다린 만큼 그대로 완료됐어요";

/** 소급 승인 — 늦게 봐도 그날로 반영된다 (ACE-6.2) */
export const backfilledNotice = "늦게 확인됐지만 한 날짜로 반영됐어요";

export const empty = {
  title: "아직 미션이 없어요",
  body: "부모님이 만들어 주시면 여기에 보여요",
};

export const noDevice = { title: "아직 준비가 안 됐어요", body: "부모님이 이 기기를 등록해 주셔야 열려요" };
export const consentRequired = { title: "보호자 동의가 필요해요", body: "부모님께 알려 주세요" };
