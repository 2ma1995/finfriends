-- 옷장 차감 트리거 — 어긋남 대장 D8
--
-- 🔴 SRS 가 비워 둔 자리다.
--    `REQ-FUNC-004` 는 *"별은 **차감형**"*, `STR-003` 은 *"옷장 — 원장 차감 기입"* 이라 적었는데
--    §6.2.2 트리거 8종은 **전부 지급용**이라 차감을 기입할 코드가 없다.
--
-- 🔴 이 코드가 P-21 을 깨지 않는 이유 — 나가는 곳이 **앱 내 재화(옷장·방 아이템)뿐**이다.
--    현금·저금통·제휴처로 나가는 코드는 여전히 존재하지 않는다.

ALTER TYPE "activity"."StarTrigger" ADD VALUE IF NOT EXISTS 'WARDROBE_SPEND';
